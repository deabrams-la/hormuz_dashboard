import './globals.css';

export const metadata = {
  title: 'Hormuz Crisis — Prolonged Disruption Model',
  description: 'Force-ranked tactical commodities trade desk',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
